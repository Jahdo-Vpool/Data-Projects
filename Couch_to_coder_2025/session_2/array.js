const ingredients = ['beef','noodles'];
ingredients[2] = 'salt'
ingredients.push('peper')
ingredients.push('cheese')
const lastIngredient = ingredients.pop()
console.log(ingredients);
console.log(lastIngredient)
console.log(ingredients.length)
